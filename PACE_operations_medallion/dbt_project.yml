name: PACE_operations_medallion

models:
  PACE_operations_medallion:

    bronze:
      +schema: BRONZE
      +materialized: table

    silver:
      +schema: SILVER
      +materialized: view

    gold:
      +schema: GOLD
      +materialized: table