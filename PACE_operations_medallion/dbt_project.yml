models:
  PACE_operations_medallion:

    +persist_docs:
      relation: true
      columns: true

    # ===============================
    # SILVER LAYER
    # ===============================
    pace_enterprise_silver_dbt_models:

      +tags: ["silver"]
      +schema: ENTERPRISE_SILVER
      +materialized: view

    # ===============================
    # GOLD LAYER
    # ===============================
    pace_gold_dbt_models:

      +tags: ["gold"]
      +materialized: table
      +schema: GOLD